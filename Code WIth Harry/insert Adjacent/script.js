// first.insertAdjacentHTML('beforebegin' , '<div class = "test"> beforebegin</div>');//added before starting of "#first"

// first.insertAdjacentHTML('beforeend' , '<div class = "test"> beforeend</div>');//added inside "#first" just before end of  "#first" , and after the content of "#first"

// first.insertAdjacentHTML('afterbegin' , '<div class = "test"> afterbegin</div>');//added inside "#first" just after , and before the content of "#first"

// first.insertAdjacentHTML('afterend' , '<div class = "test"> afterend</div>');//added after end of "#first"

// node.remove() //to remove a node
first.remove();